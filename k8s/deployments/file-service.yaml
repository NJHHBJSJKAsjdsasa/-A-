apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
  namespace: doraemon-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: file-service
  template:
    metadata:
      labels:
        app: file-service
    spec:
      containers:
      - name: file-service
        image: doraemon/file-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3006
        envFrom:
        - configMapRef:
            name: doraemon-config
        - secretRef:
            name: doraemon-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: doraemon-platform
spec:
  selector:
    app: file-service
  ports:
  - port: 3006
    targetPort: 3006
